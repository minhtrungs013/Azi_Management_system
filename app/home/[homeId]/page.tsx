

export default function HomeId({ params }: { params: { homeId: string } }) {
    console.log('aaa');
    
    // const dispatch = useDispatch<AppDispatch>();
    // const [data, setData] = useState<ProjectDetails>()
    // const refresh = useSelector((state: RootState) => state.task.refresh);

    // useEffect(() => {
    //     (async () => {
    //         const res = await dispatch(getProjectId(params.homeId));
    //         if (getProjectId.fulfilled.match(res)) {
    //             setData(res.payload);
    //         }
    //     })();
    // }, [refresh, params.homeId])

    return (
        <div >
           hihihi
        </div>
    );
}

'use client';
import { ProjectDetails } from '@/types/project';
const AnswerCall = ({ closeModal, data }: { closeModal: () => void, data: ProjectDetails | undefined }) => {


    return (
        <div className='bg-gradient-to-b from-[#1a1a29] to-[#0c0c12]  flex flex-col items-center justify-center p-4 '>
            
        </div>
    );
};

export default AnswerCall;
